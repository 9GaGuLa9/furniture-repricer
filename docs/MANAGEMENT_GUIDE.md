# 🎮 Furniture Repricer - Управління без адмін-панелі

## 📋 Три способи управління:

### 1️⃣ **Telegram Bot** (Рекомендовано) 👍
### 2️⃣ **Google Sheets Config**
### 3️⃣ **SSH + config.yaml**

---

## 💬 Спосіб 1: Telegram Bot (Найпростіше!)

### Налаштування:

1. **Запустити Telegram бота на сервері:**
```bash
sudo systemctl start repricer-telegram
sudo systemctl status repricer-telegram  # Перевірити
```

2. **Відкрити Telegram → знайти вашого бота → /start**

### Доступні команди:

```
/start - Почати роботу з ботом
/help - Список всіх команд

📊 Моніторинг:
/status - Чи запущений репрайсер зараз?
/last - Результати останнього запуску
/logs - Останні 20 рядків логів

🎮 Управління:
/run - Запустити репрайсер ЗАРАЗ (вручну)
/schedule - Переглянути розклад автозапусків
/config - Поточна конфігурація
```

### Приклади використання:

**Сценарій 1: Перевірити що все працює**
```
Ви → /status
Бот → ✅ Status: Idle
      📅 Останнє оновлення: 2024-12-16 06:15:23
      ⏰ Наступний запуск: Today at 16:00 EST
```

**Сценарій 2: Запустити вручну**
```
Ви → /run
Бот → 🚀 Репрайсер запущено!
      ⏱ Очікуваний час: 30-60 хвилин
      _Ви отримаєте сповіщення після завершення_
```

**Сценарій 3: Подивитись логи**
```
Ви → /logs
Бот → 📝 Останні логи:
      [INFO] Processing product SKU123...
      [INFO] Updated price: $149.99
      [INFO] Matched 1245 products...
```

**Переваги Telegram Bot:**
- ✅ Керування з телефону (будь-де, будь-коли)
- ✅ Миттєві сповіщення
- ✅ Не треба SSH
- ✅ Не треба VPN
- ✅ Зручний інтерфейс

---

## 📊 Спосіб 2: Google Sheets Config

### Створити аркуш "Config" в основній таблиці:

**Структура:**
```
A                       B            C
Parameter               Value        Description
----------------------------------------------------
run_enabled             TRUE         Увімкнути автозапуск
run_time_1              06:00        Перший запуск (EST)
run_time_2              16:00        Другий запуск
run_time_3              21:00        Третій запуск
scraper_emmamason       TRUE         Парсити сайт клієнта
scraper_1stopbedrooms   TRUE         Парсити конкурента 1
scraper_coleman         TRUE         Парсити конкурента 2
scraper_afa             TRUE         Парсити конкурента 3
telegram_notifications  TRUE         Надсилати сповіщення
max_retries             3            Повтори при помилках
delay_between_requests  2            Затримка (секунди)
```

### Як змінювати:

1. Відкрити Google Sheets
2. Перейти на аркуш "Config"
3. Змінити значення в колонці B
4. Зберегти (Ctrl+S)
5. **Зміни застосуються при наступному запуску!**

### Приклади:

**Вимкнути парсинг AFA (якщо сайт не працює):**
```
scraper_afa → FALSE
```

**Змінити час запуску:**
```
run_time_2 → 14:00  (замість 16:00)
```

**Збільшити затримку між запитами:**
```
delay_between_requests → 5  (замість 2)
```

**Переваги Google Sheets:**
- ✅ Знайоме середовище
- ✅ Клієнт може сам змінювати
- ✅ Не треба доступу до сервера
- ✅ Візуально зрозуміло

---

## 🖥️ Спосіб 3: SSH + config.yaml

### Для досвідчених користувачів:

1. **Підключитись до VPS:**
```bash
ssh user@your-vps-ip
cd /opt/furniture-repricer
```

2. **Редагувати конфігурацію:**
```bash
nano config.yaml
```

3. **Змінити параметри:**
```yaml
scrapers:
  coleman:
    enabled: false  # Вимкнути парсинг

pricing:
  coefficients:
    floor: 1.6  # Змінити з 1.5 на 1.6
```

4. **Зберегти:** Ctrl+X, Y, Enter

5. **Перезапустити Telegram бота (якщо треба):**
```bash
sudo systemctl restart repricer-telegram
```

### Корисні команди SSH:

**Переглянути логи:**
```bash
tail -f logs/repricer_$(date +%Y-%m-%d).log
```

**Ручний запуск:**
```bash
./run_repricer.sh
```

**Тестовий запуск (1 товар):**
```bash
./run_repricer.sh --test
```

**Перевірити cron:**
```bash
crontab -l
```

**Змінити cron розклад:**
```bash
crontab -e
```

**Переваги SSH:**
- ✅ Повний контроль
- ✅ Швидкі зміни
- ✅ Доступ до всіх файлів

---

## 🔄 Типові сценарії управління:

### Сценарій 1: Щоденна перевірка

**Через Telegram:**
```
08:00 - /status (перевірити чи пройшов ранковий запуск)
12:00 - /last (подивитись результати)
18:00 - /status (перевірити денний запуск)
```

### Сценарій 2: Конкурент недоступний

**Через Google Sheets:**
1. Відкрити аркуш Config
2. `scraper_coleman → FALSE`
3. Зберегти
4. Через Telegram: `/run` (перезапустити)

### Сценарій 3: Змінити формулу ціноутворення

**Через Google Sheets (аркуш Formula):**
1. Змінити `Floor = 1.6` (замість 1.5)
2. Змінити `Below = 2` (замість 1)
3. Зберегти
4. Наступний запуск використає нові значення

### Сценарій 4: Терміново запустити

**Через Telegram:**
```
Ви → /run
Бот → 🚀 Запущено!
     
[Через 45 хвилин]
Бот → ✅ Завершено! 
      Оновлено 234 товари
```

### Сценарій 5: Щось не працює

**Діагностика через Telegram:**
```
/status → Перевірити статус
/logs → Подивитись помилки
/last → Результати останнього запуску
```

**Якщо треба детальніше:**
```bash
ssh user@vps
tail -100 logs/repricer_*.log  # Останні 100 рядків
```

---

## 📱 Порівняльна таблиця:

| Функція | Telegram | Google Sheets | SSH |
|---------|----------|---------------|-----|
| Запустити вручну | ✅ /run | ❌ | ✅ |
| Переглянути статус | ✅ /status | ❌ | ✅ |
| Переглянути логи | ✅ /logs | ❌ | ✅ |
| Змінити параметри | ❌ | ✅ | ✅ |
| Змінити розклад | ❌ | ✅ | ✅ |
| Сповіщення | ✅ Авто | ❌ | ❌ |
| Потрібен доступ | Telegram | Google | SSH |
| Складність | ⭐ Легко | ⭐⭐ Середньо | ⭐⭐⭐ Складно |

---

## 🎯 Рекомендації:

### Для клієнта (нетехнічного):
1. **Основне:** Telegram Bot для моніторингу та ручного запуску
2. **Налаштування:** Google Sheets Config для змін параметрів
3. **Не треба:** SSH доступ

### Для адміністратора:
1. **Моніторинг:** Telegram Bot
2. **Швидкі зміни:** SSH + config.yaml
3. **Клієнт:** Google Sheets для його змін

---

## ✅ Чеклист налаштування:

- [ ] Telegram бот запущений: `systemctl status repricer-telegram`
- [ ] Відправив /start боту в Telegram
- [ ] Перевірив /status - працює
- [ ] Створив аркуш Config в Google Sheets (опціонально)
- [ ] Протестував /run - запуск працює
- [ ] Налаштував cron для автозапусків
- [ ] Отримую сповіщення після завершення

---

## 🆘 Швидка допомога:

**Бот не відповідає:**
```bash
sudo systemctl restart repricer-telegram
sudo systemctl status repricer-telegram
```

**Репрайсер не запускається:**
```bash
./run_repricer.sh --test
# Дивитись помилки
```

**Не приходять сповіщення:**
```bash
# Перевірити .env
cat .env | grep TELEGRAM
```

**Cron не спрацьовує:**
```bash
# Перевірити логи cron
tail -f /var/log/syslog | grep CRON
```

---

## 📞 Підсумок:

**Найпростіший спосіб керування:** Telegram Bot
- Все керування з телефону
- Миттєві сповіщення
- Не треба доступу до сервера

**Для зміни параметрів:** Google Sheets Config
- Знайоме середовище
- Клієнт може сам
- Візуально зрозуміло

**Немає потреби в адмін-панелі!** 🎉

Telegram Bot + Google Sheets = Повний контроль без веб-інтерфейсу
