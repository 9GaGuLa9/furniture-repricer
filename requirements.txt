# Furniture Repricer - Requirements
# Updated with Scheduler support

# Core dependencies
gspread==5.12.0
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# Web scraping
requests==2.31.0
beautifulsoup4==4.12.2
lxml==5.2.0

# Cloudflare bypass (choose one or both)
curl-cffi==0.6.2         # Recommended for best results
cloudscraper==1.2.71     # Fallback option

# Data processing
pandas==2.3.0
openpyxl==3.1.2          # For Excel file operations
python-docx==1.1.0       # For Word documents

# YAML config
PyYAML==6.0.1

# Environment variables
python-dotenv==1.2.1

# ✅ NEW: Scheduler dependencies
schedule==1.2.0          # Job scheduling
pytz==2024.1             # Timezone support

# Optional: Telegram notifications
# python-telegram-bot==20.7  # Uncomment if using Telegram
